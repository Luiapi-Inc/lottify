node_modules/
dist/
.next/
coverage/
.env
.env.*
!.env.example
apps/api/openapi/openapi.json
packages/contracts/src/generated/api.ts
*.log
.DS_Store

# Python bytecode cache
__pycache__/
*.pyc

# Hermes-local orchestration state and local policy (not project source).
# Owner-approved untrack 2026-09-18: these files stay on each host's disk and are
# no longer versioned. A specific artifact can still be committed deliberately
# with `git add -f` (e.g. a chosen report under .hermes/evidence/release/).
.hermes/
.serena/
